#include <iostream>
using namespace std;

int main() {
    string grupo, tipo, alimentacion;
    cin >> grupo >> tipo >> alimentacion;

    if (tipo == "ave") {
        if (alimentacion == "carnivoro") {
            cout << "aguia" << endl;
        } else {
            cout << "pomba" << endl;
        }
    } else if (tipo == "mamifero") {
        if (alimentacion == "onivoro") {
            cout << "homem" << endl;
        } else {
            cout << "vaca" << endl;
        }
    } else if (tipo == "inseto") {
        if (alimentacion == "hematofago") {
            cout << "pulga" << endl;
        } else {
            cout << "lagarta" << endl;
        }
    } else {
        if (alimentacion == "hematofago") {
            cout << "sanguessuga" << endl;
        } else {
            cout << "minhoca" << endl;
        }
    }

    return 0;
}
